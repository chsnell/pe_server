## This is just a stub fixture so we can do unit testing against it without
## pulling it in as a requirement, as it's a private repository.
class pe_puppetdb::pe (
  $postgres_listen_addresses = '*',
  $database_host             = 'db.example.com',
) {

}
